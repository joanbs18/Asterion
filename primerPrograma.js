function sumar(a, b) {
    return a + b;
}

// Llamada a la función y almacenamiento del resultado en una variable
const resultado = sumar(3, 5);

// Imprimir el resultado
console.log(resultado); // Imprime 8 en la consola
